jar {
    enabled = true
    archiveClassifier = ''
    version = project.version
}

bootJar {
    enabled = false
}

dependencies {
    implementation "com.alibaba:transmittable-thread-local:${rootProject.ttlVer}"
}