---
phase: 10-admin-frontend-ui
plan: '02'
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ui/src/views/permission/request/detail.vue
  - ui/src/views/permission/request/detail.ts
  - ui/src/router/menuRoutes.ts
autonomous: true
requirements:
  - PAPR-02
  - PAPR-04
  - PAPR-05

must_haves:
  truths:
    - "Admin can navigate to /permission/request/detail?id=XXX and see request detail with item list"
    - "Each PENDING item shows checkbox for selection; AUTO_APPROVED items show no action"
    - "Approve modal allows selecting expressions and priority for selected items"
    - "Reject modal allows entering required reject reason (200 char limit)"
  artifacts:
    - path: ui/src/views/permission/request/detail.vue
      provides: Request detail page with item list, approve modal, reject modal
      min_lines: 250
    - path: ui/src/views/permission/request/detail.ts
      provides: Detail page logic and state management
      min_lines: 100
    - path: ui/src/router/menuRoutes.ts
      provides: Route entry for /permission/request/detail
      modifies: adds route entry
  key_links:
    - from: detail.vue
      to: ui/src/api/adminPermissionRequest.ts
      via: approveRequest, rejectRequest calls
      pattern: approveRequest|rejectRequest
    - from: detail.vue
      to: ui/src/api/permission.ts
      via: getPermissionExpList for expression selector
      pattern: getPermissionExpList
    - from: list page (10-01)
      to: detail page
      via: router.push(/permission/request/detail?id=)
      pattern: /permission/request/detail
---

<objective>
实现申请详情页面（路由 `/permission/request/detail?id=`），包含权限子项列表，每个子项支持勾选审批。实现批准弹窗（限制条件选择）和拒绝弹窗（拒绝理由输入）。
</objective>

<execution_context>
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@ui/src/api/adminPermissionRequest.ts
@ui/src/views/permission/authorize/index.vue
@ui/src/views/user/home/components/RequestRecords.vue
@ui/src/api/permission.ts
@auth-server/src/main/java/cn/opensrcdevelop/auth/controller/user/UserCenterPermissionRequestController.java

# API Response Structures (from Phase 4-6 backend):

## PermissionRequestDetailDto (GET /user-center/permissions/requests/{id}):
```typescript
interface PermissionRequestDetailDto {
  requestId: string;
  userId: string;
  reason: string;
  requestTime: string;
  items: {
    permissionId: string;
    permissionName: string;
    permissionCode: string;
    status: 'PENDING' | 'APPROVED' | 'REJECTED';
    rejectReason?: string;
  }[];
}
```

Note: This endpoint is in the user center controller. For admin access, the frontend may need to call this endpoint directly (admin has elevated permissions). If authorization fails, a new admin detail endpoint would need to be added.

## ApproveRequestDto (D-13 to D-17):
```typescript
interface ApproveRequestDto {
  itemIds?: string[];        // 可选，不传则批准所有 PENDING
  expressionIds?: string[];  // 限制条件ID列表
  priority?: number;         // -1最低, 0低, 1中, 2高, 3最高
}
```

## RejectRequestDto (D-18 to D-21):
```typescript
interface RejectRequestDto {
  itemIds?: string[];        // 可选，不传则拒绝所有 PENDING
  rejectReason: string;      // 必填，拒绝理由
}
```

# Expression List API (for approve modal - D-14):
- `GET /permissions/expressions` — 获取限制条件列表（参考 ui/src/api/permission.ts 的 getPermissionExpList）

# Status Badge Colors (D-12):
```typescript
const STATUS_COLOR_MAP: Record<string, string> = {
  PENDING: 'arcoblue',
  APPROVED: 'green',
  REJECTED: 'red',
};
```

# Key Implementation Decisions:
- **Detail page is a ROUTE, not a modal** — route `/permission/request/detail?id=`
- D-10: 展示：申请信息、权限列表（每个子项含名称/代码/状态）、审批结果
- D-11: **每个子项独立审批** — 支持部分批准、部分拒绝
  - 每个 PENDING 状态的子项显示勾选框
  - 顶部有「批准选中」「拒绝选中」批量操作按钮
  - 也支持对单个子项单独操作
- D-12: AUTO_APPROVED 状态的子项**不显示操作按钮**（已自动批准）
- D-14: 弹窗中设置**限制条件**（多选下拉框）和**优先级**（可选）
- D-15: 条件/优先级**适用于所有被批准的子项**
- D-19: 拒绝理由：**文本输入框**，必填，有字数限制（如 200 字）
- D-20: 拒绝理由**适用于所有被拒绝的子项**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create detail page route and API function</name>
  <files>ui/src/router/menuRoutes.ts, ui/src/api/adminPermissionRequest.ts</files>
  <action>
在 `ui/src/router/menuRoutes.ts` 中添加详情页路由（路由级别的页面，不是弹窗）：

**新增代码（在 `/permission/request` 路由之后）：**
```typescript
{
  path: "/permission/request/detail",
  component: () => import("@/views/permission/request/detail.vue"),
  meta: {
    title: "申请详情",
    visible: () => false,  // 不在菜单显示，通过列表页跳转
  },
},
```

同时在 `adminPermissionRequest.ts` 中添加获取详情的 API 函数：

```typescript
/**
 * 获取权限申请详情
 * 对应 GET /user-center/permissions/requests/{id}
 * 注意：admin 调用此接口需要后端开放权限或新增 admin 专用接口
 */
export function getRequestDetail(requestId: string) {
  return apiRequest.get({
    url: `/user-center/permissions/requests/${requestId}`,
  });
}
```
</action>
  <verify>
    <automated>grep -c "/permission/request/detail" ui/src/router/menuRoutes.ts && grep -c "getRequestDetail" ui/src/api/adminPermissionRequest.ts</automated>
  </verify>
  <done>Detail page route added at /permission/request/detail, API function added</done>
</task>

<task type="auto">
  <name>Task 2: Create detail page with item list and checkboxes</name>
  <files>ui/src/views/permission/request/detail.vue, ui/src/views/permission/request/detail.ts</files>
  <action>
创建 `ui/src/views/permission/request/detail.vue` 和 `ui/src/views/permission/request/detail.ts` 组件。

**页面结构：**
- 顶部返回按钮 + 页面标题「申请详情」
- 申请信息区域：申请ID、申请人、申请时间、状态、申请理由
- 权限子项列表（带勾选框）：每个 PENDING 子项显示复选框
- 批量操作按钮：「批准选中」「拒绝选中」
- 批准弹窗：限制条件多选 + 优先级选择
- 拒绝弹窗：拒绝理由文本输入（200字限制）

**detail.ts 状态定义：**
```typescript
import { ref, reactive, computed } from 'vue';
import { useRoute } from 'vue-router';
import { Message } from '@arco-design/web-vue';
import { getRequestDetail, approveRequest, rejectRequest } from '@/api/adminPermissionRequest';
import { getPermissionExpList } from '@/api/permission';

// 路由
const route = useRoute();
const requestId = computed(() => route.query.id as string);

// 页面状态
const loading = ref(false);
const detail = ref<any>(null);

// 选中项
const selectedItemIds = ref<string[]>([]);

// 批准弹窗
const approveModalVisible = ref(false);
const approveLoading = ref(false);
const approveForm = reactive({
  itemIds: [] as string[],
  expressionIds: [] as string[],
  priority: undefined as number | undefined,
});

// 拒绝弹窗
const rejectModalVisible = ref(false);
const rejectLoading = ref(false);
const rejectForm = reactive({
  itemIds: [] as string[],
  rejectReason: '',
});

// 限制条件列表
const expressionList = ref<any[]>([]);

// computed
const hasPendingItems = computed(() => {
  return detail.value?.items?.some((item: any) => item.status === 'PENDING') ?? false;
});

const pendingItemCount = computed(() => {
  return detail.value?.items?.filter((item: any) => item.status === 'PENDING').length ?? 0;
});

const allSelected = computed(() => {
  const pendingItems = detail.value?.items?.filter((item: any) => item.status === 'PENDING') ?? [];
  return pendingItems.length > 0 && selectedItemIds.value.length === pendingItems.length;
});

const indeterminate = computed(() => {
  const pendingItems = detail.value?.items?.filter((item: any) => item.status === 'PENDING') ?? [];
  return selectedItemIds.value.length > 0 && selectedItemIds.value.length < pendingItems.length;
});

// 方法
const loadDetail = async () => {
  if (!requestId.value) return;
  loading.value = true;
  try {
    const res = await getRequestDetail(requestId.value);
    if (res.data) {
      detail.value = res.data;
    }
  } catch (err: any) {
    Message.error(err.message || '加载详情失败');
  } finally {
    loading.value = false;
  }
};

const loadExpressionList = async () => {
  try {
    const res = await getPermissionExpList({});
    if (res.data) {
      expressionList.value = res.data;
    }
  } catch (err) {
    console.error('加载限制条件失败', err);
  }
};

const handleItemCheck = (itemId: string) => {
  const index = selectedItemIds.value.indexOf(itemId);
  if (index > -1) {
    selectedItemIds.value.splice(index, 1);
  } else {
    selectedItemIds.value.push(itemId);
  }
};

const handleSelectAllChange = (checked: boolean) => {
  if (checked) {
    selectedItemIds.value = detail.value?.items
      ?.filter((item: any) => item.status === 'PENDING')
      ?.map((item: any) => item.itemId) ?? [];
  } else {
    selectedItemIds.value = [];
  }
};

const handleBatchApprove = () => {
  if (selectedItemIds.value.length === 0) {
    Message.warning('请选择要批准的权限');
    return;
  }
  approveForm.itemIds = [...selectedItemIds.value];
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
  loadExpressionList();
  approveModalVisible.value = true;
};

const handleSingleApprove = (itemId: string) => {
  approveForm.itemIds = [itemId];
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
  loadExpressionList();
  approveModalVisible.value = true;
};

const handleApproveSubmit = async (done: Function) => {
  approveLoading.value = true;
  try {
    await approveRequest(requestId.value, {
      itemIds: approveForm.itemIds,
      expressionIds: approveForm.expressionIds.length > 0 ? approveForm.expressionIds : undefined,
      priority: approveForm.priority,
    });
    Message.success('批准成功');
    approveModalVisible.value = false;
    await loadDetail();
    selectedItemIds.value = [];
    done(true);
  } catch (err: any) {
    Message.error(err.message || '批准失败');
    done(false);
  } finally {
    approveLoading.value = false;
  }
};

const handleBatchReject = () => {
  if (selectedItemIds.value.length === 0) {
    Message.warning('请选择要拒绝的权限');
    return;
  }
  rejectForm.itemIds = [...selectedItemIds.value];
  rejectForm.rejectReason = '';
  rejectModalVisible.value = true;
};

const handleSingleReject = (itemId: string) => {
  rejectForm.itemIds = [itemId];
  rejectForm.rejectReason = '';
  rejectModalVisible.value = true;
};

const handleRejectSubmit = async (done: Function) => {
  if (!rejectForm.rejectReason || rejectForm.rejectReason.trim() === '') {
    Message.warning('请输入拒绝理由');
    done(false);
    return;
  }
  if (rejectForm.rejectReason.length > 200) {
    Message.warning('拒绝理由不能超过200字');
    done(false);
    return;
  }
  rejectLoading.value = true;
  try {
    await rejectRequest(requestId.value, {
      itemIds: rejectForm.itemIds,
      rejectReason: rejectForm.rejectReason,
    });
    Message.success('拒绝成功');
    rejectModalVisible.value = false;
    await loadDetail();
    selectedItemIds.value = [];
    done(true);
  } catch (err: any) {
    Message.error(err.message || '拒绝失败');
    done(false);
  } finally {
    rejectLoading.value = false;
  }
};

export default {
  // 状态
  loading,
  detail,
  selectedItemIds,
  approveModalVisible,
  approveLoading,
  approveForm,
  rejectModalVisible,
  rejectLoading,
  rejectForm,
  expressionList,
  // computed
  hasPendingItems,
  pendingItemCount,
  allSelected,
  indeterminate,
  // 方法
  loadDetail,
  handleItemCheck,
  handleSelectAllChange,
  handleBatchApprove,
  handleSingleApprove,
  handleApproveSubmit,
  handleBatchReject,
  handleSingleReject,
  handleRejectSubmit,
};
```

**detail.vue 模板（关键部分）：**
```vue
<template>
  <div class="request-detail">
    <!-- 返回按钮和标题 -->
    <div class="detail-header">
      <a-button @click="$router.back()">
        <template #icon><icon-left /></template>
        返回
      </a-button>
      <div class="title">申请详情</div>
    </div>

    <a-spin :loading="loading" style="width: 100%">
      <a-form v-if="detail" layout="vertical">
        <!-- 申请信息 -->
        <a-divider orientation="left">申请信息</a-divider>
        <a-row :gutter="24">
          <a-col :span="8">
            <a-form-item label="申请ID">
              <copy-text :text="detail.requestId" />
            </a-form-item>
          </a-col>
          <a-col :span="8">
            <a-form-item label="申请时间">
              {{ detail.requestTime }}
            </a-form-item>
          </a-col>
          <a-col :span="8">
            <a-form-item label="状态">
              <a-tag :color="STATUS_COLOR_MAP[detail.status]">
                {{ STATUS_TEXT_MAP[detail.status] }}
              </a-tag>
            </a-form-item>
          </a-col>
        </a-row>
        <a-form-item label="申请理由">
          {{ detail.reason || '-' }}
        </a-form-item>

        <!-- 权限列表 -->
        <a-divider orientation="left">
          申请权限
          <a-checkbox
            v-if="hasPendingItems"
            style="margin-left: 8px"
            :indeterminate="indeterminate"
            :checked="allSelected"
            @change="handleSelectAllChange"
          >
            全选
          </a-checkbox>
        </a-divider>

        <!-- 批量操作按钮 -->
        <div v-if="hasPendingItems" style="margin-bottom: 12px">
          <a-space>
            <a-button
              type="primary"
              :disabled="selectedItemIds.length === 0"
              @click="handleBatchApprove"
            >
              批准选中 ({{ selectedItemIds.length }})
            </a-button>
            <a-button
              type="primary"
              status="danger"
              :disabled="selectedItemIds.length === 0"
              @click="handleBatchReject"
            >
              拒绝选中 ({{ selectedItemIds.length }})
            </a-button>
          </a-space>
        </div>

        <!-- 权限项表格 -->
        <a-table :data="detail.items" :pagination="false" size="small">
          <template #columns>
            <a-table-column :width="40" v-if="hasPendingItems">
              <template #cell="{ record }">
                <a-checkbox
                  v-if="record.status === 'PENDING'"
                  :value="record.itemId"
                  :model-value="selectedItemIds.includes(record.itemId)"
                  @change="handleItemCheck(record.itemId)"
                />
              </template>
            </a-table-column>
            <a-table-column title="权限名称" data-index="permissionName" />
            <a-table-column title="权限标识" data-index="permissionCode" :width="200" />
            <a-table-column title="状态" :width="100" align="center">
              <template #cell="{ record }">
                <a-tag :color="STATUS_COLOR_MAP[record.status]">
                  {{ STATUS_TEXT_MAP[record.status] }}
                </a-tag>
              </template>
            </a-table-column>
            <a-table-column title="拒绝理由" :width="150">
              <template #cell="{ record }">
                {{ record.rejectReason || '-' }}
              </template>
            </a-table-column>
            <a-table-column title="操作" :width="150" align="center" v-if="hasPendingItems">
              <template #cell="{ record }">
                <template v-if="record.status === 'PENDING'">
                  <a-button type="text" size="small" @click="handleSingleApprove(record.itemId)">
                    批准
                  </a-button>
                  <a-button type="text" size="small" status="danger" @click="handleSingleReject(record.itemId)">
                    拒绝
                  </a-button>
                </template>
                <span v-else style="color: #999">-</span>
              </template>
            </a-table-column>
          </template>
        </a-table>
      </a-form>
    </a-spin>

    <!-- 批准弹窗 -->
    <a-modal
      v-model:visible="approveModalVisible"
      title="批准权限申请"
      :width="500"
      @before-ok="handleApproveSubmit"
    >
      <a-form :model="approveForm" layout="vertical">
        <a-form-item label="将批准的权限">
          <a-text>{{ approveForm.itemIds.length }} 个权限</a-text>
        </a-form-item>
        <a-form-item label="限制条件">
          <a-select
            v-model:model-value="approveForm.expressionIds"
            placeholder="请选择限制条件（可选）"
            multiple
            allow-clear
            allow-search
          >
            <a-option v-for="exp in expressionList" :key="exp.id" :value="exp.id">
              {{ exp.name }}
            </a-option>
          </a-select>
        </a-form-item>
        <a-form-item label="优先级">
          <a-select
            v-model:model-value="approveForm.priority"
            placeholder="请选择优先级（可选）"
            allow-clear
          >
            <a-option :value="-1">最低</a-option>
            <a-option :value="0">低</a-option>
            <a-option :value="1">中</a-option>
            <a-option :value="2">高</a-option>
            <a-option :value="3">最高</a-option>
          </a-select>
        </a-form-item>
      </a-form>
    </a-modal>

    <!-- 拒绝弹窗 -->
    <a-modal
      v-model:visible="rejectModalVisible"
      title="拒绝权限申请"
      :width="500"
      @before-ok="handleRejectSubmit"
    >
      <a-form :model="rejectForm" layout="vertical">
        <a-form-item label="将拒绝的权限">
          <a-text>{{ rejectForm.itemIds.length }} 个权限</a-text>
        </a-form-item>
        <a-form-item
          label="拒绝理由"
          required
          :validate-status="rejectForm.rejectReason.length > 200 ? 'error' : ''"
          :help="rejectForm.rejectReason.length > 200 ? '拒绝理由不能超过200字' : ''"
        >
          <a-textarea
            v-model:model-value="rejectForm.rejectReason"
            placeholder="请输入拒绝理由（必填）"
            :maxlength="200"
            show-word-limit
            :rows="3"
          />
        </a-form-item>
      </a-form>
    </a-modal>
  </div>
</template>

<script setup lang="ts">
import detail from './detail';
const STATUS_COLOR_MAP: Record<string, string> = {
  PENDING: 'arcoblue',
  APPROVED: 'green',
  REJECTED: 'red',
};
const STATUS_TEXT_MAP: Record<string, string> = {
  PENDING: '待审批',
  APPROVED: '已批准',
  REJECTED: '已拒绝',
};
</script>
```

**关键实现要点：**
1. 详情页是独立路由 `/permission/request/detail?id=`，不是弹窗
2. 从列表页通过 `router.push('/permission/request/detail?id=' + requestId)` 跳转
3. PENDING 状态子项显示勾选框，AUTO_APPROVED 不显示操作
4. 批量操作按钮：「批准选中」「拒绝选中」
5. 单个操作：行内「批准」「拒绝」按钮
</action>
  <verify>
    <automated>grep -c "handleBatchApprove\|handleBatchReject\|handleSingleApprove\|handleSingleReject" ui/src/views/permission/request/detail.vue && grep -c "selectedItemIds\|approveModalVisible\|rejectModalVisible" ui/src/views/permission/request/detail.ts</automated>
  </verify>
  <done>Detail page created at /permission/request/detail with item checkboxes and approve/reject modals</done>
</task>

<task type="auto">
  <name>Task 3: Update list page to navigate to detail page</name>
  <files>ui/src/views/permission/request/index.vue, ui/src/views/permission/request/index.ts</files>
  <action>
修改 Wave 1 创建的列表页，将「查看详情」按钮的弹窗逻辑改为路由跳转。

**修改 handleViewDetail 方法（index.ts）：**
```typescript
// 查看详情 - 改为跳转到详情页
const handleViewDetail = (record: any) => {
  router.push(`/permission/request/detail?id=${record.requestId}`);
};
```

**更新 import（index.ts）：**
```typescript
import { useRouter } from 'vue-router';
const router = useRouter();
```

**修改「查看详情」按钮（index.vue）：**
```vue
<a-button type="text" size="small" @click="handleViewDetail(scope.record)">
  查看详情
</a-button>
```

**删除之前添加的详情弹窗相关代码**（如果有的话），确保列表页只做跳转，不显示弹窗。
</action>
  <verify>
    <automated>grep -c "router.push\|/permission/request/detail" ui/src/views/permission/request/index.vue && grep -c "router.push\|/permission/request/detail" ui/src/views/permission/request/index.ts</automated>
  </verify>
  <done>List page "查看详情" button now navigates to detail page instead of showing modal</done>
</task>

</tasks>

<verification>
- Detail page route added at /permission/request/detail
- Detail page shows request info, item list with checkboxes
- PENDING items show checkbox and approve/reject buttons
- Approve modal has expression multi-select and priority dropdown
- Reject modal has required textarea with 200 char limit
- List page "查看详情" navigates to detail page
</verification>

<success_criteria>
1. Navigating to /permission/request/detail?id=XXX shows request detail
2. PENDING items have checkboxes for batch selection
3. "批准选中" opens approve modal with expression selector and priority
4. "拒绝选中" opens reject modal with required reason textarea (200 char limit)
5. Single-item approve/reject buttons work for individual items
6. After approval/rejection, the detail page refreshes to reflect changes
7. List page "查看详情" navigates to detail page via router
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-frontend-ui/10-02-SUMMARY.md`
</output>
