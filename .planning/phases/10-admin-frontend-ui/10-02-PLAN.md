---
phase: 10-admin-frontend-ui
plan: '02'
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ui/src/views/permission/request/index.vue
  - ui/src/views/permission/request/index.ts
autonomous: true
requirements:
  - PAPR-02
  - PAPR-04
  - PAPR-05

must_haves:
  truths:
    - "Admin can see request detail modal with: 申请信息, 权限列表(每个子项含名称/代码/状态), 审批结果"
    - "Each PENDING item shows checkbox for selection (AUTO_APPROVED items have no action)"
    - "Admin can approve single item with optional expression conditions and priority"
    - "Admin can reject single item with required reject reason (200 char limit)"
  artifacts:
    - path: ui/src/views/permission/request/index.vue
      provides: Detail modal with item list, approve modal, reject modal
      extends: 10-01
      min_lines: 200
  key_links:
    - from: index.vue
      to: ui/src/api/adminPermissionRequest.ts
      via: approveRequest, rejectRequest calls
      pattern: approveRequest|rejectRequest
    - from: approve modal
      to: ui/src/api/permission.ts
      via: getPermissionExpList for expression selector
      pattern: getPermissionExpList
---

<objective>
实现申请详情弹窗，包含权限子项列表，每个子项支持勾选审批（部分批准/部分拒绝）。实现批准弹窗（限制条件选择）和拒绝弹窗（拒绝理由输入）。
</objective>

<execution_context>
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@ui/src/api/adminPermissionRequest.ts
@ui/src/views/permission/authorize/index.vue
@ui/src/views/user/home/components/RequestRecords.vue
@ui/src/api/permission.ts

# API Response Structures (from Phase 6 backend):

## PermissionRequestDetailDto (from getRequestDetail - Phase 6):
```typescript
interface PermissionRequestDetailDto {
  requestId: string;
  requestTime: string;
  reason: string;
  status: 'PENDING' | 'APPROVED' | 'REJECTED' | 'AUTO_APPROVED';
  items: {
    itemId: string;
    permissionId: string;
    permissionName: string;
    permissionCode: string;
    status: 'PENDING' | 'APPROVED' | 'REJECTED';
    rejectReason?: string;
  }[];
  approvalInfo?: {
    approvedAt: string;
    approvedBy: string;
    rejectReason?: string;
  };
}
```

## ApproveRequestDto (D-13 to D-17):
```typescript
interface ApproveRequestDto {
  itemIds?: string[];        // 可选，不传则批准所有 PENDING
  expressionIds?: string[];  // 限制条件ID列表
  priority?: number;         // -1最低, 0低, 1中, 2高, 3最高
}
```

## RejectRequestDto (D-18 to D-21):
```typescript
interface RejectRequestDto {
  itemIds?: string[];        // 可选，不传则拒绝所有 PENDING
  rejectReason: string;      // 必填，拒绝理由
}
```

# Expression List API (for approve modal - D-14):
- `GET /permissions/expressions` — 获取限制条件列表（参考 ui/src/api/permission.ts 的 getPermissionExpList）

# Status Badge Colors (D-12):
```typescript
const STATUS_COLOR_MAP: Record<string, string> = {
  PENDING: 'arcoblue',
  APPROVED: 'green',
  REJECTED: 'red',
  AUTO_APPROVED: 'purple',
};
```

# Key Implementation Decisions:
- D-09: 点击「查看详情」打开弹窗
- D-10: 展示：申请信息、权限列表（每个子项含名称/代码/状态）、审批结果
- D-11: **每个子项独立审批** — 支持部分批准、部分拒绝
  - 每个 PENDING 状态的子项显示勾选框
  - 顶部有「批准选中」「拒绝选中」批量操作按钮
  - 也支持对单个子项单独操作
- D-12: AUTO_APPROVED 状态的子项**不显示操作按钮**（已自动批准）
- D-14: 弹窗中设置**限制条件**（多选下拉框）和**优先级**（可选）
- D-15: 条件/优先级**适用于所有被批准的子项**
- D-19: 拒绝理由：**文本输入框**，必填，有字数限制（如 200 字）
- D-20: 拒绝理由**适用于所有被拒绝的子项**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detail modal with item list and selection checkboxes</name>
  <files>ui/src/views/permission/request/index.vue</files>
  <action>
在 `ui/src/views/permission/request/index.vue` 中添加详情弹窗组件。

**新增状态（在 index.ts 中）：**
```typescript
// 详情弹窗
const detailModalVisible = ref(false);
const detailLoading = ref(false);
const requestDetail = ref<any>(null);

// 选中项（用于批量操作）
const selectedItemIds = ref<string[]>([]);

// 批准弹窗
const approveModalVisible = ref(false);
const approveLoading = ref(false);
const approveForm = reactive({
  itemIds: [] as string[],
  expressionIds: [] as string[],
  priority: undefined as number | undefined,
});

// 拒绝弹窗
const rejectModalVisible = ref(false);
const rejectLoading = ref(false);
const rejectForm = reactive({
  itemIds: [] as string[],
  rejectReason: '',
});
```

**详情弹窗模板（添加到 template 中）：**
```vue
<!-- 详情弹窗 -->
<a-modal
  v-model:visible="detailModalVisible"
  title="申请详情"
  :width="700"
  :footer="null"
  @cancel="handleCloseDetail"
>
  <a-spin :loading="detailLoading" style="width: 100%">
    <a-form v-if="requestDetail" layout="vertical">
      <!-- 申请信息 -->
      <a-divider orientation="left">申请信息</a-divider>
      <a-row :gutter="24">
        <a-col :span="12">
          <a-form-item label="申请ID">
            <copy-text :text="requestDetail.requestId" />
          </a-form-item>
        </a-col>
        <a-col :span="12">
          <a-form-item label="申请时间">
            {{ requestDetail.requestTime }}
          </a-form-item>
        </a-col>
      </a-row>
      <a-row :gutter="24">
        <a-col :span="12">
          <a-form-item label="状态">
            <a-tag :color="STATUS_COLOR_MAP[requestDetail.status]">
              {{ STATUS_TEXT_MAP[requestDetail.status] }}
            </a-tag>
          </a-form-item>
        </a-col>
        <a-col :span="12">
          <a-form-item label="申请理由">
            {{ requestDetail.reason || '-' }}
          </a-form-item>
        </a-col>
      </a-row>

      <!-- 权限列表（对应 D-10, D-11） -->
      <a-divider orientation="left">
        申请权限
        <a-checkbox
          v-if="hasPendingItems"
          style="margin-left: 8px"
          :indeterminate="indeterminate"
          :checked="allSelected"
          @change="handleSelectAllChange"
        >
          全选
        </a-checkbox>
      </a-divider>

      <!-- 批量操作按钮（对应 D-11） -->
      <div v-if="hasPendingItems" style="margin-bottom: 12px">
        <a-space>
          <a-button type="primary" :disabled="selectedItemIds.length === 0" @click="handleBatchApprove">
            批准选中 ({{ selectedItemIds.length }})
          </a-button>
          <a-button type="primary" status="danger" :disabled="selectedItemIds.length === 0" @click="handleBatchReject">
            拒绝选中 ({{ selectedItemIds.length }})
          </a-button>
        </a-space>
      </div>

      <a-table
        :data="requestDetail.items"
        :bordered="false"
        :pagination="false"
        size="small"
        @selection-change="handleSelectionChange"
      >
        <template #columns>
          <a-table-column :width="40" v-if="hasPendingItems">
            <template #cell="{ record }">
              <a-checkbox
                v-if="record.status === 'PENDING'"
                :value="record.itemId"
                :model-value="selectedItemIds.includes(record.itemId)"
                @change="handleItemCheck(record.itemId)"
              />
            </template>
          </a-table-column>
          <a-table-column title="权限名称" data-index="permissionName" />
          <a-table-column title="权限标识" data-index="permissionCode" :width="200" />
          <a-table-column title="状态" :width="100" align="center">
            <template #cell="{ record }">
              <a-tag :color="STATUS_COLOR_MAP[record.status]">
                {{ STATUS_TEXT_MAP[record.status] }}
              </a-tag>
            </template>
          </a-table-column>
          <a-table-column title="拒绝理由" :width="150">
            <template #cell="{ record }">
              {{ record.rejectReason || '-' }}
            </template>
          </a-table-column>
          <a-table-column title="操作" :width="150" align="center" v-if="hasPendingItems">
            <template #cell="{ record }">
              <template v-if="record.status === 'PENDING'">
                <a-button type="text" size="small" @click="handleSingleApprove(record.itemId)">
                  批准
                </a-button>
                <a-button type="text" size="small" status="danger" @click="handleSingleReject(record.itemId)">
                  拒绝
                </a-button>
              </template>
              <span v-else style="color: #999">-</span>
            </template>
          </a-table-column>
        </template>
      </a-table>

      <!-- 审批结果（对应 D-10） -->
      <template v-if="requestDetail.approvalInfo">
        <a-divider orientation="left">审批结果</a-divider>
        <a-row :gutter="24">
          <a-col :span="12">
            <a-form-item label="审批时间">
              {{ requestDetail.approvalInfo.approvedAt }}
            </a-form-item>
          </a-col>
          <a-col :span="12">
            <a-form-item label="审批人">
              {{ requestDetail.approvalInfo.approvedBy }}
            </a-form-item>
          </a-col>
        </a-row>
      </template>
    </a-form>
  </a-spin>
</a-modal>
```

**computed 属性（添加到 index.ts）：**
```typescript
// 是否有待审批的子项
const hasPendingItems = computed(() => {
  return requestDetail.value?.items?.some((item: any) => item.status === 'PENDING') ?? false;
});

// 全选状态
const allSelected = computed(() => {
  const pendingItems = requestDetail.value?.items?.filter((item: any) => item.status === 'PENDING') ?? [];
  return pendingItems.length > 0 && selectedItemIds.value.length === pendingItems.length;
});

// 不确定状态
const indeterminate = computed(() => {
  const pendingItems = requestDetail.value?.items?.filter((item: any) => item.status === 'PENDING') ?? [];
  return selectedItemIds.value.length > 0 && selectedItemIds.value.length < pendingItems.length;
});
```

**新增方法（添加到 index.ts）：**
```typescript
// 查看详情
const handleViewDetail = (requestId: string) => {
  detailModalVisible.value = true;
  detailLoading.value = true;
  selectedItemIds.value = [];
  // 调用详情API（Phase 6 应该有此接口，如果没有则需要确认）
  // 目前使用前端模拟数据结构
};

// 关闭详情
const handleCloseDetail = () => {
  detailModalVisible.value = false;
  requestDetail.value = null;
  selectedItemIds.value = [];
};

// 选择变化
const handleSelectionChange = (keys: string[]) => {
  selectedItemIds.value = keys;
};

// 全选变化
const handleSelectAllChange = (checked: boolean) => {
  if (checked) {
    selectedItemIds.value = requestDetail.value?.items
      ?.filter((item: any) => item.status === 'PENDING')
      ?.map((item: any) => item.itemId) ?? [];
  } else {
    selectedItemIds.value = [];
  }
};

// 单个勾选
const handleItemCheck = (itemId: string) => {
  const index = selectedItemIds.value.indexOf(itemId);
  if (index > -1) {
    selectedItemIds.value.splice(index, 1);
  } else {
    selectedItemIds.value.push(itemId);
  }
};
```

**关键实现要点：**
1. 参考 `ui/src/views/user/home/components/RequestRecords.vue` 的详情弹窗模式
2. D-11: 每个 PENDING 状态子项显示勾选框，AUTO_APPROVED 不显示操作
3. 顶部有「批准选中」「拒绝选中」批量操作按钮
4. 也支持对单个子项单独操作（行内批准/拒绝按钮）
5. 勾选框联动全选状态（indeterminate 计算属性）
</action>
  <verify>
    <automated>grep -c "handleBatchApprove\|handleBatchReject\|handleSingleApprove\|handleSingleReject" ui/src/views/permission/request/index.vue && grep -c "selectedItemIds\|indeterminate\|allSelected" ui/src/views/permission/request/index.vue</automated>
  </verify>
  <done>Detail modal shows items with checkboxes, batch approve/reject buttons, and single-item actions</done>
</task>

<task type="auto">
  <name>Task 2: Implement approve modal with expression selector and priority</name>
  <files>ui/src/views/permission/request/index.vue, ui/src/views/permission/request/index.ts</files>
  <action>
在详情弹窗中添加批准弹窗，实现限制条件多选和优先级选择。

**批准弹窗模板（添加到 template 中，放在详情弹窗之后）：**
```vue
<!-- 批准弹窗（对应 D-13 至 D-17） -->
<a-modal
  v-model:visible="approveModalVisible"
  title="批准权限申请"
  :width="500"
  @before-ok="handleApproveSubmit"
  @cancel="handleApproveCancel"
>
  <a-form :model="approveForm" layout="vertical">
    <a-form-item label="将批准的权限">
      <a-text>{{ selectedItemIds.length }} 个权限</a-text>
    </a-form-item>

    <!-- 限制条件（对应 D-14） -->
    <a-form-item label="限制条件">
      <a-select
        v-model:model-value="approveForm.expressionIds"
        placeholder="请选择限制条件（可选）"
        multiple
        allow-clear
        allow-search
      >
        <a-option
          v-for="exp in expressionList"
          :key="exp.id"
          :value="exp.id"
        >
          {{ exp.name }}
        </a-option>
      </a-select>
    </a-form-item>

    <!-- 优先级（对应 D-14） -->
    <a-form-item label="优先级">
      <a-select
        v-model:model-value="approveForm.priority"
        placeholder="请选择优先级（可选）"
        allow-clear
      >
        <a-option :value="-1">最低</a-option>
        <a-option :value="0">低</a-option>
        <a-option :value="1">中</a-option>
        <a-option :value="2">高</a-option>
        <a-option :value="3">最高</a-option>
      </a-select>
    </a-form-item>
  </a-form>
</a-modal>
```

**新增状态和方法（添加到 index.ts）：**
```typescript
// 限制条件列表
const expressionList = ref<any[]>([]);

// 批准弹窗方法
const handleBatchApprove = () => {
  approveForm.itemIds = [...selectedItemIds.value];
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
  // 加载限制条件列表
  loadExpressionList();
  approveModalVisible.value = true;
};

const handleSingleApprove = (itemId: string) => {
  approveForm.itemIds = [itemId];
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
  loadExpressionList();
  approveModalVisible.value = true;
};

const loadExpressionList = async () => {
  try {
    const res = await getPermissionExpList({});
    if (res.data) {
      expressionList.value = res.data;
    }
  } catch (err) {
    console.error('加载限制条件失败', err);
  }
};

const handleApproveSubmit = async (done: Function) => {
  if (approveForm.itemIds.length === 0) {
    Message.warning('请选择要批准的权限');
    done(false);
    return;
  }

  approveLoading.value = true;
  try {
    await approveRequest(requestDetail.value.requestId, {
      itemIds: approveForm.itemIds,
      expressionIds: approveForm.expressionIds.length > 0 ? approveForm.expressionIds : undefined,
      priority: approveForm.priority,
    });
    Message.success('批准成功');
    approveModalVisible.value = false;
    // 刷新列表
    await handleRefreshList();
    done(true);
  } catch (err: any) {
    Message.error(err.message || '批准失败');
    done(false);
  } finally {
    approveLoading.value = false;
  }
};

const handleApproveCancel = () => {
  approveModalVisible.value = false;
  approveForm.itemIds = [];
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
};
```

**参考 ui/src/views/permission/authorize/index.vue 的限制条件下拉框模式（D-14）：**
- a-select multiple 模式
- 复用 getPermissionExpList API（从 ui/src/api/permission.ts 导入）

**关键实现要点：**
1. D-14: 限制条件使用 a-select multiple，参考 authorize/index.vue
2. D-15: expressionIds 和 priority 适用于所有被批准的子项
3. 不选限制条件时，后端会使用默认行为
4. 提交后刷新列表和详情数据
</action>
  <verify>
    <automated>grep -c "approveModalVisible\|expressionIds\|priority" ui/src/views/permission/request/index.vue && grep -c "approveRequest" ui/src/views/permission/request/index.ts</automated>
  </verify>
  <done>Approve modal shows expression multi-select and priority dropdown, submits to approveRequest API</done>
</task>

<task type="auto">
  <name>Task 3: Implement reject modal with required reason input</name>
  <files>ui/src/views/permission/request/index.vue, ui/src/views/permission/request/index.ts</files>
  <action>
在详情弹窗中添加拒绝弹窗，实现必填的拒绝理由输入。

**拒绝弹窗模板（添加到 template 中）：**
```vue
<!-- 拒绝弹窗（对应 D-18 至 D-21） -->
<a-modal
  v-model:visible="rejectModalVisible"
  title="拒绝权限申请"
  :width="500"
  @before-ok="handleRejectSubmit"
  @cancel="handleRejectCancel"
>
  <a-form :model="rejectForm" layout="vertical">
    <a-form-item label="将拒绝的权限">
      <a-text>{{ rejectForm.itemIds.length }} 个权限</a-text>
    </a-form-item>

    <!-- 拒绝理由（对应 D-19） -->
    <a-form-item
      label="拒绝理由"
      required
      :validate-status="rejectForm.rejectReason.length > 200 ? 'error' : ''"
      :help="rejectForm.rejectReason.length > 200 ? '拒绝理由不能超过200字' : ''"
    >
      <a-textarea
        v-model:model-value="rejectForm.rejectReason"
        placeholder="请输入拒绝理由（必填）"
        :maxlength="200"
        show-word-limit
        :rows="3"
      />
    </a-form-item>
  </a-form>
</a-modal>
```

**新增状态和方法（添加到 index.ts）：**
```typescript
// 拒绝弹窗方法
const handleBatchReject = () => {
  rejectForm.itemIds = [...selectedItemIds.value];
  rejectForm.rejectReason = '';
  rejectModalVisible.value = true;
};

const handleSingleReject = (itemId: string) => {
  rejectForm.itemIds = [itemId];
  rejectForm.rejectReason = '';
  rejectModalVisible.value = true;
};

const handleRejectSubmit = async (done: Function) => {
  if (!rejectForm.rejectReason || rejectForm.rejectReason.trim() === '') {
    Message.warning('请输入拒绝理由');
    done(false);
    return;
  }

  if (rejectForm.rejectReason.length > 200) {
    Message.warning('拒绝理由不能超过200字');
    done(false);
    return;
  }

  if (rejectForm.itemIds.length === 0) {
    Message.warning('请选择要拒绝的权限');
    done(false);
    return;
  }

  rejectLoading.value = true;
  try {
    await rejectRequest(requestDetail.value.requestId, {
      itemIds: rejectForm.itemIds,
      rejectReason: rejectForm.rejectReason,
    });
    Message.success('拒绝成功');
    rejectModalVisible.value = false;
    // 刷新列表
    await handleRefreshList();
    done(true);
  } catch (err: any) {
    Message.error(err.message || '拒绝失败');
    done(false);
  } finally {
    rejectLoading.value = false;
  }
};

const handleRejectCancel = () => {
  rejectModalVisible.value = false;
  rejectForm.itemIds = [];
  rejectForm.rejectReason = '';
};
```

**刷新列表方法（添加到 index.ts）：**
```typescript
// 刷新列表
const handleRefreshList = async () => {
  // 根据当前 Tab 刷新对应列表
  // 如果详情关闭则刷新列表
  if (!detailModalVisible.value) {
    // 刷新当前列表
  }
};
```

**关键实现要点：**
1. D-19: 拒绝理由使用 a-textarea，必填，有 200 字限制
2. D-19: show-word-limit 显示字数统计
3. D-20: rejectReason 适用于所有被拒绝的子项
4. 提交前校验理由不为空、不超过 200 字
5. 提交后刷新列表和详情数据
</action>
  <verify>
    <automated>grep -c "rejectModalVisible\|rejectReason\|handleRejectSubmit" ui/src/views/permission/request/index.vue && grep -c "rejectRequest" ui/src/views/permission/request/index.ts</automated>
  </verify>
  <done>Reject modal shows required textarea with 200 char limit, submits to rejectRequest API</done>
</task>

</tasks>

<verification>
- Detail modal shows request info, item list with checkboxes, approval result
- PENDING items show checkbox and approve/reject buttons
- AUTO_APPROVED items show no action buttons
- Approve modal has expression multi-select and priority dropdown
- Reject modal has required textarea with 200 char limit
- Both modals call correct API endpoints
</verification>

<success_criteria>
1. Clicking "查看详情" opens modal showing: 申请信息, 权限列表, 审批结果
2. PENDING items have checkboxes for batch selection
3. "批准选中" opens approve modal with expression selector and priority
4. "拒绝选中" opens reject modal with required reason textarea (200 char limit)
5. Single-item approve/reject buttons work for individual items
6. After approval/rejection, the detail modal updates to reflect changes
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-frontend-ui/10-02-SUMMARY.md`
</output>
