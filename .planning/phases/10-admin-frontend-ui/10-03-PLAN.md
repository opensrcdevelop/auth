---
phase: 10-admin-frontend-ui
plan: '03'
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - ui/src/views/permission/request/detail.vue
  - ui/src/views/permission/request/detail.ts
autonomous: true
requirements:
  - PAPR-02
  - PAPR-04
  - PAPR-05

must_haves:
  truths:
    - "Admin can approve some items while leaving others in PENDING state"
    - "Admin can reject some items while leaving others in PENDING state"
    - "Approving selected items does NOT reject unselected items"
    - "Rejecting selected items does NOT approve unselected items"
    - "After partial approve or reject, unprocessed items remain PENDING for subsequent operations"
  artifacts:
    - path: ui/src/views/permission/request/detail.vue
      provides: Detail page with partial approve/reject workflow
      extends: 10-02
  key_links:
    - from: detail.vue
      to: ui/src/api/adminPermissionRequest.ts
      via: approveRequest, rejectRequest calls
      pattern: approveRequest|rejectRequest
---

<objective>
实现同一申请单内的部分批准+部分拒绝工作流。关键：批准选中的子项不会影响未选中的子项（它们保持 PENDING），拒绝同理。这允许管理员分多次操作处理同一申请单。
</objective>

<execution_context>
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@ui/src/views/permission/request/detail.vue
@ui/src/views/permission/request/detail.ts
@ui/src/api/adminPermissionRequest.ts

# Key Behavior Clarification (from user feedback):

**Critical:** Approving selected items JUST approves those — it does NOT reject the unselected items. The unselected items stay PENDING.

Similarly, rejecting selected items JUST rejects those — it does NOT approve the unselected items. The unselected items stay PENDING.

This allows the admin to process a request over multiple operations:
1. First approve some items
2. Then reject others later
3. Or first reject some items, then approve others later
4. Items not yet processed remain PENDING until explicitly approved or rejected

# Current State (from Plan 10-02):
- Detail page with item list and checkboxes already exists
- Batch approve/reject buttons already exist
- After each approve/reject, the detail is reloaded

# What to Add in Plan 10-03:
1. "批准其余" / "拒绝其余" buttons — for quick operations on unselected items
2. Clearer UX: show which items are already approved/rejected vs still pending
3. Optional: "一键批准全部" / "一键拒绝全部" for quick processing

# NOT in Scope:
- Cross-request batch operations (Phase 7 skipped)
- Auto-rejection of unselected items (NOT how partial approve works)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "批准其余" and "拒绝其余" buttons for unselected items</name>
  <files>ui/src/views/permission/request/detail.vue, ui/src/views/permission/request/detail.ts</files>
  <action>
在详情页的批量操作按钮区域添加「批准其余」「拒绝其余」按钮，方便管理员快速处理未选中的子项。

**在 detail.ts 中添加新方法：**

```typescript
// 批准其余未选中的待处理项
const handleApproveRemaining = () => {
  const remainingIds = detail.value?.items
    ?.filter((item: any) => item.status === 'PENDING' && !selectedItemIds.value.includes(item.itemId))
    ?.map((item: any) => item.itemId) ?? [];

  if (remainingIds.length === 0) {
    Message.warning('没有其余需要批准的权限');
    return;
  }

  approveForm.itemIds = remainingIds;
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
  loadExpressionList();
  approveModalVisible.value = true;
};

// 拒绝其余未选中的待处理项
const handleRejectRemaining = () => {
  const remainingIds = detail.value?.items
    ?.filter((item: any) => item.status === 'PENDING' && !selectedItemIds.value.includes(item.itemId))
    ?.map((item: any) => item.itemId) ?? [];

  if (remainingIds.length === 0) {
    Message.warning('没有其余需要拒绝的权限');
    return;
  }

  rejectForm.itemIds = remainingIds;
  rejectForm.rejectReason = '';
  rejectModalVisible.value = true;
};
```

**更新 export default：**
```typescript
export default {
  // ... existing exports ...
  // 新增方法
  handleApproveRemaining,
  handleRejectRemaining,
};
```

**在 detail.vue 中更新批量操作按钮区域：**

```vue
<!-- 批量操作按钮 -->
<div v-if="hasPendingItems" style="margin-bottom: 12px">
  <a-space direction="vertical" fill>
    <a-space>
      <a-button
        type="primary"
        :disabled="selectedItemIds.length === 0"
        @click="handleBatchApprove"
      >
        批准选中 ({{ selectedItemIds.length }})
      </a-button>
      <a-button
        type="primary"
        status="danger"
        :disabled="selectedItemIds.length === 0"
        @click="handleBatchReject"
      >
        拒绝选中 ({{ selectedItemIds.length }})
      </a-button>
    </a-space>
    <a-space v-if="selectedItemIds.length > 0 && selectedItemIds.length < pendingItemCount">
      <a-text type="secondary">
        还有 {{ pendingItemCount - selectedItemIds.length }} 个待处理
      </a-text>
      <a-button
        size="small"
        @click="handleApproveRemaining"
      >
        批准其余
      </a-button>
      <a-button
        size="small"
        status="danger"
        @click="handleRejectRemaining"
      >
        拒绝其余
      </a-button>
    </a-space>
  </a-space>
</div>
```

**关键说明：**
- 「批准选中」：只批准当前勾选的项
- 「批准其余」：批准所有未勾选的 PENDING 项
- 两者互斥，管理员根据需要选择
- 操作后刷新详情，未操作的项保持 PENDING
</action>
  <verify>
    <automated>grep -c "handleApproveRemaining\|handleRejectRemaining" ui/src/views/permission/request/detail.vue && grep -c "handleApproveRemaining\|handleRejectRemaining" ui/src/views/permission/request/detail.ts</automated>
  </verify>
  <done>"批准其余" and "拒绝其余" buttons added for unselected items</done>
</task>

<task type="auto">
  <name>Task 2: Add quick action buttons for all pending items</name>
  <files>ui/src/views/permission/request/detail.vue, ui/src/views/permission/request/detail.ts</files>
  <action>
添加「一键批准全部」「一键拒绝全部」按钮，提供快速处理选项。

**在 detail.ts 中添加新方法：**

```typescript
// 一键批准所有待处理项
const handleApproveAll = () => {
  const allPendingIds = detail.value?.items
    ?.filter((item: any) => item.status === 'PENDING')
    ?.map((item: any) => item.itemId) ?? [];

  if (allPendingIds.length === 0) {
    Message.warning('没有待处理的权限');
    return;
  }

  approveForm.itemIds = allPendingIds;
  approveForm.expressionIds = [];
  approveForm.priority = undefined;
  loadExpressionList();
  approveModalVisible.value = true;
};

// 一键拒绝所有待处理项
const handleRejectAll = () => {
  const allPendingIds = detail.value?.items
    ?.filter((item: any) => item.status === 'PENDING')
    ?.map((item: any) => item.itemId) ?? [];

  if (allPendingIds.length === 0) {
    Message.warning('没有待处理的权限');
    return;
  }

  rejectForm.itemIds = allPendingIds;
  rejectForm.rejectReason = '';
  rejectModalVisible.value = true;
};
```

**更新 export default：**
```typescript
export default {
  // ... existing exports ...
  // 新增方法
  handleApproveAll,
  handleRejectAll,
};
```

**在 detail.vue 中添加快速操作按钮（在表格下方）：**

```vue
<!-- 快速操作按钮（在权限列表表格下方） -->
<div v-if="hasPendingItems" style="margin-top: 12px; text-align: right">
  <a-space>
    <a-button type="text" size="small" @click="handleApproveAll">
      一键批准全部 ({{ pendingItemCount }})
    </a-button>
    <a-button type="text" size="small" status="danger" @click="handleRejectAll">
      一键拒绝全部 ({{ pendingItemCount }})
    </a-button>
  </a-space>
</div>
```

**更新 computed 属性以跟踪待处理项数量：**
确保 `pendingItemCount` 正确计算：

```typescript
const pendingItemCount = computed(() => {
  return detail.value?.items?.filter((item: any) => item.status === 'PENDING').length ?? 0;
});
```

**关键说明：**
- 「一键批准全部」：一次性批准所有 PENDING 项
- 「一键拒绝全部」：一次性拒绝所有 PENDING 项
- 这些是快捷操作，与分别勾选效果相同但更便捷
- 操作后刷新详情
</action>
  <verify>
    <automated>grep -c "handleApproveAll\|handleRejectAll" ui/src/views/permission/request/detail.vue && grep -c "handleApproveAll\|handleRejectAll" ui/src/views/permission/request/detail.ts</automated>
  </verify>
  <done>Quick action buttons for approve all / reject all added</done>
</task>

</tasks>

<verification>
- Detail page has "批准选中", "拒绝选中" buttons
- Detail page has "批准其余", "拒绝其余" buttons for unselected items
- Detail page has "一键批准全部", "一键拒绝全部" quick action buttons
- After any operation, detail refreshes and shows latest item statuses
- Unprocessed items remain PENDING (not auto-approved or auto-rejected)
</verification>

<success_criteria>
1. Admin can approve some items, reject others, and leave remaining in PENDING
2. Approving selected items does NOT affect unselected items (they stay PENDING)
3. Rejecting selected items does NOT affect unselected items (they stay PENDING)
4. "批准其余" and "拒绝其余" buttons handle unselected pending items
5. "一键批准全部" and "一键拒绝全部" provide quick batch operations
6. After any operation, detail refreshes to show updated item statuses
7. Items already processed (APPROVED/REJECTED) cannot be re-operated
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-frontend-ui/10-03-SUMMARY.md`
</output>
