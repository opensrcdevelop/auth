---
phase: 10-admin-frontend-ui
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/api/adminPermissionRequest.ts
  - ui/src/views/permission/request/index.vue
  - ui/src/views/permission/request/index.ts
  - ui/src/router/menuRoutes.ts
autonomous: true
requirements:
  - PAPR-01
  - PAPR-06

must_haves:
  truths:
    - "Admin can see pending requests list with columns: 申请ID, 申请人, 申请时间, 状态, 权限数量, 操作"
    - "Admin can see all requests list with multi-select status filter"
    - "Status badges show correct colors and Chinese text (待审批=arcoblue, 已批准=green, 已拒绝=red, 自动批准=purple)"
    - "Pagination works with page=1, size=15 default"
  artifacts:
    - path: ui/src/api/adminPermissionRequest.ts
      provides: Admin API functions for permission request operations
      min_lines: 60
    - path: ui/src/views/permission/request/index.vue
      provides: Permission request approval management page
      min_lines: 150
    - path: ui/src/views/permission/request/index.ts
      provides: Page logic and state management
      min_lines: 80
    - path: ui/src/router/menuRoutes.ts
      provides: Menu route for permission request approval
      modifies: adds route entry
  key_links:
    - from: ui/src/views/permission/request/index.vue
      to: ui/src/api/adminPermissionRequest.ts
      via: getPendingList, getAllList imports
      pattern: getPendingList|getAllList
    - from: ui/src/router/menuRoutes.ts
      to: ui/src/views/permission/request/index.vue
      via: route path /permission/request
      pattern: /permission/request
---

<objective>
实现管理员审批管理页面，包含待审批列表和全部申请列表（按状态筛选）。创建 adminPermissionRequest API 模块，挂载菜单路由。
</objective>

<execution_context>
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@ui/src/views/permission/resource/index.vue
@ui/src/views/user/home/components/RequestRecords.vue
@ui/src/api/permission.ts
@ui/src/util/tool.ts

# API Response Structures (from Phase 5-6 backend):

## PermissionRequestListItemDto (from GET /admin/permissions/requests/pending and GET /admin/permissions/requests/all):
```typescript
interface PermissionRequestListItemDto {
  requestId: string;
  requestTime: string;
  reason: string;
  itemCount: number;
}
```

## Admin API Endpoints (Phase 5-6):
- `GET /admin/permissions/requests/pending?page=1&size=15` — 待审批列表 (PENDING + AUTO_APPROVED)
- `GET /admin/permissions/requests/all?page=1&size=15&status=PENDING` — 全部申请 (可选 status 筛选)
- `POST /admin/permissions/requests/{id}/approve` — 批准
- `POST /admin/permissions/requests/{id}/reject` — 拒绝

# Status Badge Colors (D-12, UI-SPEC):
```typescript
const STATUS_COLOR_MAP: Record<string, string> = {
  PENDING: 'arcoblue',       // #165DFF
  APPROVED: 'green',          // #00B42A
  REJECTED: 'red',            // #E8353E
  AUTO_APPROVED: 'purple',    // #7C3AED
};

const STATUS_TEXT_MAP: Record<string, string> = {
  PENDING: '待审批',
  APPROVED: '已批准',
  REJECTED: '已拒绝',
  AUTO_APPROVED: '自动批准',
};
```

# Menu Route Decision (D-01 to D-03):
- 挂在「权限管理」菜单下作为子菜单
- 新增路由 `/permission/request` 对应审批管理页面
- 与现有的资源权限（`/permission/resource`）、限制条件（`/permission/expression`）并列
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create adminPermissionRequest API module</name>
  <files>ui/src/api/adminPermissionRequest.ts</files>
  <action>
创建 `ui/src/api/adminPermissionRequest.ts` 文件，包含以下 API 函数：

```typescript
import apiRequest from "@/util/apiRequest";

/**
 * 获取待审批权限申请列表
 * 对应 D-05: GET /admin/permissions/requests/pending
 * 返回 PENDING 和 AUTO_APPROVED 状态的申请
 */
export function getPendingList(params: { page: number; size: number }) {
  return apiRequest.get({
    url: "/admin/permissions/requests/pending",
    params,
  });
}

/**
 * 获取所有权限申请列表
 * 对应 D-05: GET /admin/permissions/requests/all?status=
 * 支持按状态筛选，不传 status 返回全部
 */
export function getAllList(params: {
  page: number;
  size: number;
  status?: 'PENDING' | 'APPROVED' | 'REJECTED' | 'AUTO_APPROVED';
}) {
  return apiRequest.get({
    url: "/admin/permissions/requests/all",
    params,
  });
}

/**
 * 批准权限申请
 * 对应 D-17: POST /admin/permissions/requests/{id}/approve
 */
export function approveRequest(requestId: string, data: {
  itemIds?: string[];
  expressionIds?: string[];
  priority?: number;
}) {
  return apiRequest.post({
    url: `/admin/permissions/requests/${requestId}/approve`,
    data,
  });
}

/**
 * 拒绝权限申请
 * 对应 D-21: POST /admin/permissions/requests/{id}/reject
 */
export function rejectRequest(requestId: string, data: {
  itemIds?: string[];
  rejectReason: string;
}) {
  return apiRequest.post({
    url: `/admin/permissions/requests/${requestId}/reject`,
    data,
  });
}
```

参考 `ui/src/api/permission.ts` 的函数结构，使用相同的 apiRequest 调用模式。
</action>
  <verify>
    <automated>grep -l "getPendingList\|getAllList\|approveRequest\|rejectRequest" ui/src/api/adminPermissionRequest.ts</automated>
  </verify>
  <done>API module created with all 4 functions exported</done>
</task>

<task type="auto">
  <name>Task 2: Create permission request approval page</name>
  <files>ui/src/views/permission/request/index.vue, ui/src/views/permission/request/index.ts</files>
  <action>
创建 `ui/src/views/permission/request/index.vue` 和 `ui/src/views/permission/request/index.ts` 组件，实现审批管理页面。

**页面结构：**
- 顶部标题区域：「权限申请审批」
- Tab 切换：「待审批」(pending) / 「全部申请」(all)
- 待审批 Tab：调用 getPendingList，分页展示
- 全部申请 Tab：顶部状态多选筛选器 + 分页表格
- 表格列：申请ID、申请时间、申请理由（截断）、权限数量、状态、操作
- 操作列：「查看详情」按钮

**状态多选筛选器（对应 D-04 至 D-06）：**
```typescript
// 状态选项
const statusOptions = [
  { label: '待审批', value: 'PENDING' },
  { label: '已批准', value: 'APPROVED' },
  { label: '已拒绝', value: 'REJECTED' },
  { label: '自动批准', value: 'AUTO_APPROVED' },
];

// 多选绑定
const selectedStatuses = ref<string[]>([]);
```

**表格列定义（对应 D-07）：**
```typescript
const columns = [
  { title: '申请ID', dataIndex: 'requestId', width: 200 },
  { title: '申请时间', dataIndex: 'requestTime', width: 180 },
  { title: '申请理由', dataIndex: 'reason', ellipsis: true },
  { title: '权限数量', dataIndex: 'itemCount', width: 100, align: 'center' as const },
  { title: '状态', dataIndex: 'status', width: 100, align: 'center' as const },
  { title: '操作', width: 100, align: 'center' as const },
];
```

**Status Badge 颜色（对应 D-12）：**
```typescript
const STATUS_COLOR_MAP: Record<string, string> = {
  PENDING: 'arcoblue',
  APPROVED: 'green',
  REJECTED: 'red',
  AUTO_APPROVED: 'purple',
};

const STATUS_TEXT_MAP: Record<string, string> = {
  PENDING: '待审批',
  APPROVED: '已批准',
  REJECTED: '已拒绝',
  AUTO_APPROVED: '自动批准',
};
```

**分页配置（对应 D-08）：**
```typescript
const pagination = reactive({
  current: 1,
  pageSize: 15,
  total: 0,
});
```

**关键实现要点：**
1. 参考 `ui/src/views/permission/resource/index.vue` 的页面布局模式
2. Tab 使用 a-tabs 组件，参考 `ui/src/views/user/home/index.vue`
3. 表格使用 a-table，状态列使用 a-tag 渲染颜色
4. 待审批 Tab 默认展示，PENDING + AUTO_APPROVED 状态
5. 全部申请 Tab 支持多选状态筛选，不选则显示全部
6. 分页使用 a-table 内置分页，pageSize 默认 15

**index.ts 导出：**
```typescript
export default {
  // 状态
  loading,
  pendingList,
  allList,
  pagination,
  selectedStatuses,
  statusOptions,
  columns,
  STATUS_COLOR_MAP,
  STATUS_TEXT_MAP,
  // 方法
  handleTabChange,
  handleLoadPendingList,
  handleLoadAllList,
  handlePageChange,
  handlePageSizeChange,
  handleViewDetail,
};
```
</action>
  <verify>
    <automated>grep -c "a-tabs\|a-table\|getPendingList\|getAllList" ui/src/views/permission/request/index.vue && grep -c "STATUS_COLOR_MAP\|STATUS_TEXT_MAP" ui/src/views/permission/request/index.vue</automated>
  </verify>
  <done>Permission request approval page displays pending/all lists with status badges and pagination</done>
</task>

<task type="auto">
  <name>Task 3: Add menu route for permission request approval</name>
  <files>ui/src/router/menuRoutes.ts</files>
  <action>
在 `ui/src/router/menuRoutes.ts` 中添加权限申请审批菜单路由。

**修改位置：** 在 permission.children 数组中，添加新的子路由

**新增代码（在 `/permission/expression/template` 路由之后）：**

```typescript
{
  path: "/permission/request",
  component: () => import("@/views/permission/request/index.vue"),
  meta: {
    title: "权限申请审批",
    visible: () => true,
  },
},
```

**完整的 permission.children 应该包含：**
```typescript
children: [
  {
    path: "/permission/resource",
    component: () => import("@/views/permission/resource/index.vue"),
    meta: {
      title: "资源权限",
      visible: () => true,
    },
  },
  {
    path: "/permission/expression",
    component: () => import("@/views/permission/expression/index.vue"),
    meta: {
      title: "限制条件",
      visible: () => true,
    },
  },
  {
    path: "/permission/expression/template",
    component: () => import("@/views/permission/expression/template/index.vue"),
    meta: {
      title: "限制条件模板",
      visible: () => true,
    },
  },
  {
    path: "/permission/request",
    component: () => import("@/views/permission/request/index.vue"),
    meta: {
      title: "权限申请审批",
      visible: () => true,
    },
  },
],
```

**参考 D-01 至 D-03：**
- 挂在「权限管理」菜单下作为子菜单
- 与现有的资源权限（`/permission/resource`）、限制条件（`/permission/expression`）并列
</action>
  <verify>
    <automated>grep -c "/permission/request" ui/src/router/menuRoutes.ts && grep -c "权限申请审批" ui/src/router/menuRoutes.ts</automated>
  </verify>
  <done>Menu route added under 权限管理 menu at path /permission/request</done>
</task>

</tasks>

<verification>
- adminPermissionRequest.ts exports 4 functions: getPendingList, getAllList, approveRequest, rejectRequest
- Permission request page contains a-tabs with pending/all tabs, status filter, and pagination
- Status badges use correct color mapping
- Menu route added at /permission/request under 权限管理
</verification>

<success_criteria>
1. Admin can see "权限申请审批" menu under "权限管理"
2. Clicking the menu shows page with tabs: "待审批" and "全部申请"
3. Pending tab shows PENDING + AUTO_APPROVED requests with pagination
4. All tab shows all requests with multi-select status filter
5. Table columns are: 申请ID, 申请时间, 申请理由, 权限数量, 状态, 操作
6. Status badges show correct colors and Chinese text
7. Clicking "查看详情" opens detail modal (implemented in Plan 10-02)
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-frontend-ui/10-01-SUMMARY.md`
</output>
