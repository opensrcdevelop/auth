---
phase: 03-user-request-submission
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/PermissionRequestService.java
  - auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/impl/PermissionRequestServiceImpl.java
  - auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/service/permission/request/PermissionRequestServiceTest.java
  - auth-server/src/main/java/cn/opensrcdevelop/auth/controller/user/UserCenterPermissionRequestController.java
  - auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/controller/UserCenterPermissionRequestControllerTest.java
  - auth-server/src/main/resources/application-authorize.properties
autonomous: true
requirements:
  - PREQ-03
  - PAUT-02
  - PAUT-03
  - PAUD-01

must_haves:
  truths:
    - "用户调用 POST /user-center/permissions/requests 提交申请，返回 requestId"
    - "开启自动批准的权限提交后 PermissionRequestItem.autoApproved=true，权限直接写入 t_authorize"
    - "非自动批准的权限 PermissionRequestItem.autoApproved=false，PermissionRequest.status=PENDING"
    - "全部权限自动批准时 PermissionRequest.status=AUTO_APPROVED，否则为 PENDING"
    - "存在 PENDING 或 AUTO_APPROVED 状态的重复申请时提交被拒绝"
    - "提交操作记录审计日志（@Audit 注解生效）"
    - "所有数据库操作在同一个 @Transactional 内，任何失败导致全部回滚"
  artifacts:
    - path: auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/PermissionRequestService.java
      provides: 申请提交服务接口
      exports: [submitRequest]
    - path: auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/impl/PermissionRequestServiceImpl.java
      provides: 申请提交服务实现（含事务、自动批准、审计）
    - path: auth-server/src/main/java/cn/opensrcdevelop/auth/controller/user/UserCenterPermissionRequestController.java
      provides: POST /user-center/permissions/requests 端点
    - path: auth-server/src/main/resources/application-authorize.properties
      provides: submitPermissionRequest 权限码注册
      contains: submitPermissionRequest
  key_links:
    - from: UserCenterPermissionRequestController.submitRequest
      to: PermissionRequestService.submitRequest
      via: 构造器注入调用
      pattern: "permissionRequestService\\.submitRequest"
    - from: PermissionRequestServiceImpl.submitRequest
      to: AuthorizeService.authorize
      via: 自动批准时调用（同一事务内）
      pattern: "authorizeService\\.authorize"
    - from: PermissionRequestServiceImpl.submitRequest
      to: PermissionAutoApproveService.isEnabled
      via: 每个 permissionId 判断是否自动批准
      pattern: "permissionAutoApproveService\\.isEnabled"
    - from: PermissionRequestServiceImpl.submitRequest
      to: PermissionRequestRepository.hasActivePendingRequest
      via: 提交前重复申请检查
      pattern: "permissionRequestRepository\\.hasActivePendingRequest"
---

<objective>
实现权限申请提交的核心服务逻辑和 HTTP 控制器。

Purpose: 完成 Phase 3 的核心功能交付。用户可以通过 POST /user-center/permissions/requests 提交批量权限申请，自动批准的权限即时写入 t_authorize，非自动批准的权限保持 PENDING 状态等待管理员审批，完整审计日志记录每次提交。

Output:
- PermissionRequestService 接口 + PermissionRequestServiceImpl（含 @Transactional、@Audit、自动批准逻辑）
- PermissionRequestServiceTest（单元测试，TDD 方式）
- UserCenterPermissionRequestController（POST /user-center/permissions/requests）
- UserCenterPermissionRequestControllerTest（控制器测试）
- application-authorize.properties 新增 submitPermissionRequest 权限码
</objective>

<execution_context>
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.planning/phases/03-user-request-submission/03-CONTEXT.md
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.planning/phases/03-user-request-submission/03-RESEARCH.md
@/Users/lee0407/dev/projs/auth/worktrees/permission-approval-module/.planning/phases/03-user-request-submission/03-01-SUMMARY.md

<interfaces>
<!-- 执行者必须读取的关键合约（由 Plan 01 创建，Plan 02 依赖） -->

<!-- PermissionRequestStatusEnum（Plan 01 创建） -->
```java
// cn.opensrcdevelop.auth.biz.constants.PermissionRequestStatusEnum
PENDING("PENDING", "待审批"),
APPROVED("APPROVED", "已批准"),
REJECTED("REJECTED", "已拒绝"),
AUTO_APPROVED("AUTO_APPROVED", "自动批准");
// getter: getCode()
```

<!-- PermissionRequestCreateDto（Plan 01 创建） -->
```java
// cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestCreateDto
@NotEmpty private List<String> permissionIds;
@NotBlank private String reason;
```

<!-- PermissionRequestResponseDto（Plan 01 创建） -->
```java
// cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestResponseDto
private String requestId;
private int autoApprovedCount;
private int pendingCount;
```

<!-- PermissionAutoApproveService（Plan 01 创建） -->
```java
// cn.opensrcdevelop.auth.biz.service.permission.request.PermissionAutoApproveService
boolean isEnabled(String permissionId);
```

<!-- PermissionRequestRepository（Plan 01 扩展） -->
```java
// cn.opensrcdevelop.auth.biz.repository.permission.request.PermissionRequestRepository
boolean hasActivePendingRequest(String userId, List<String> permissionIds);
```

<!-- ResourceType.PERMISSION_REQUEST（Plan 01 新增） -->
```java
// cn.opensrcdevelop.auth.audit.enums.ResourceType
PERMISSION_REQUEST("019f0001-0000-7000-8000-000000000001", "权限申请");
```

<!-- AuthorizeService.authorize（现有） -->
```java
// cn.opensrcdevelop.auth.biz.service.auth.AuthorizeService
// 实现含 @CacheEvict(cacheNames = CacheConstants.CACHE_CURRENT_USER_PERMISSIONS, allEntries = true)
void authorize(AuthorizeRequestDto requestDto);
```

<!-- AuthorizeRequestDto 构造（写入 t_authorize 所需最小配置） -->
```java
AuthorizeRequestDto authDto = new AuthorizeRequestDto();
authDto.setUserIds(List.of(userId));              // 当前用户
authDto.setPermissionIds(autoApprovedPermIds);    // 自动批准的权限ID列表
// expressionIds 和 priority 保持 null（Phase 3 不设置限制条件和优先级）
```

<!-- AuthUtil.getCurrentUserId（现有） -->
```java
// cn.opensrcdevelop.auth.biz.util.AuthUtil
public static String getCurrentUserId()
// 从 SecurityContext 获取当前登录用户ID，不接受客户端传入
```

<!-- CommonUtil.getUUIDV7String（现有） -->
```java
// cn.opensrcdevelop.common.util.CommonUtil
public static String getUUIDV7String()
// 生成 UUID v7 字符串，用于 requestId 和 itemId
```

<!-- @Audit 注解 API（现有，参考 AuthorizeServiceImpl） -->
```java
import cn.opensrcdevelop.auth.audit.annotation.Audit;
import cn.opensrcdevelop.auth.audit.enums.AuditType;
import cn.opensrcdevelop.auth.audit.enums.ResourceType;
import cn.opensrcdevelop.auth.audit.enums.SysOperationType;
// 用法：
@Audit(
    type = AuditType.USER_OPERATION,
    resource = ResourceType.PERMISSION_REQUEST,
    sysOperation = SysOperationType.CREATE,
    success = "提交了权限申请（{{ #result.requestId }}）",
    fail = "提交权限申请失败"
)
// 注意：#result 指方法返回值 PermissionRequestResponseDto
```

<!-- PermissionRequestMapper / PermissionRequestItemMapper（现有） -->
```java
// extends BaseMapper<PermissionRequest> / BaseMapper<PermissionRequestItem>
// 使用 super.insert(entity) 或 mapper.insert(entity) 插入记录
// BaseEntity 字段（createTime, createBy, version, deleted）由 MyBatis-Plus FieldFill 自动填充
```

<!-- 现有 PermissionController 结构（Controller 模式参考） -->
```java
@Tag(name = "API-XXX", description = "接口-XXX")
@RestController
@RestResponse                // common 模块的统一响应包装注解
@RequestMapping("/xxx")
@RequiredArgsConstructor
public class XxxController {
    // @Authorize({"permissionCode"}) 方法级权限控制
    // @PostMapping / @GetMapping 等
}
```

<!-- 现有测试模式（PermissionControllerTest 参考） -->
```java
@WebMvcTest(controllers = XxxController.class)
class XxxControllerTest {
    @Autowired private MockMvc mockMvc;
    @MockBean private XxxService xxxService;
    @Test @WithMockUser void testXxx() throws Exception { ... }
}
// PermissionServiceTest 模式（@SpringBootTest + @MockBean）
@SpringBootTest
class XxxServiceTest {
    @MockBean private SomeDependency dep;
    @Autowired private XxxServiceImpl service;
}
```

<!-- application-authorize.properties 新增权限码格式 -->
```properties
# 格式：auth.client.authorize.{codeName}.permission={resourceId}:{group}:{action}
# 现有示例：
auth.client.authorize.getUserPermissions.permission=52cb8d26-a352-4e5c-99a7-d52b8afff3b1:user:permissions
# 新增（权限申请，任何已认证用户可调用）：
auth.client.authorize.submitPermissionRequest.permission=52cb8d26-a352-4e5c-99a7-d52b8afff3b1:permission:request:submit
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PermissionRequestService 接口 + PermissionRequestServiceImpl（含 @Transactional、自动批准、@Audit）</name>
  <files>
    auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/PermissionRequestService.java,
    auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/impl/PermissionRequestServiceImpl.java,
    auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/service/permission/request/PermissionRequestServiceTest.java
  </files>

  <read_first>
    - auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/auth/impl/AuthorizeServiceImpl.java（@Audit 注解用法参考，第 48-56 行）
    - auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/entity/permission/request/PermissionRequest.java（实体字段）
    - auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/entity/permission/request/PermissionRequestItem.java（实体字段）
    - auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/service/permission/PermissionServiceTest.java（@SpringBootTest + @MockBean 测试模式参考）
    - auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/util/AuthUtil.java（getCurrentUserId 签名）
  </read_first>

  <behavior>
    - 测试 1: submitRequest_成功提交全部PENDING申请
      输入：permissionIds=["p1","p2"]，reason="业务需要"，两个权限均无自动批准配置
      期望：返回 PermissionRequestResponseDto，requestId 非 null，autoApprovedCount=0，pendingCount=2
      期望数据库状态：PermissionRequest.status="PENDING"，两个 PermissionRequestItem.autoApproved=false

    - 测试 2: submitRequest_自动批准权限即时写入授权
      输入：permissionIds=["p1","p2"]，"p1" 开启自动批准，"p2" 未开启
      期望：autoApprovedCount=1，pendingCount=1，PermissionRequest.status="PENDING"（混合状态）
      期望：AuthorizeService.authorize() 被调用一次，userIds=["currentUser"]，permissionIds=["p1"]

    - 测试 3: submitRequest_全部自动批准时status为AUTO_APPROVED
      输入：permissionIds=["p1"]，"p1" 开启自动批准
      期望：autoApprovedCount=1，pendingCount=0，PermissionRequest.status="AUTO_APPROVED"

    - 测试 4: submitRequest_存在重复PENDING申请时抛出异常
      输入：userId="user1"，permissionIds=["p1"]，用户已有 PENDING 申请包含 "p1"
      期望：hasActivePendingRequest 返回 true → 方法抛出 RuntimeException（或业务异常）

    - 测试 5: submitRequest_不传入reason时（理由为空）
      不在 Service 层测试此场景（由控制器 @Valid 拦截），标记为 N/A
  </behavior>

  <action>
**TDD 执行顺序：先写测试（RED），再实现（GREEN），最后检查。**

**步骤 1：创建测试骨架（RED）**

创建目录：`auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/service/permission/request/`

创建 `PermissionRequestServiceTest.java`，使用 `@SpringBootTest` + `@MockBean` 模式，mock 所有外部依赖（PermissionRequestMapper、PermissionRequestItemMapper、PermissionAutoApproveService、AuthorizeService、PermissionRequestRepository、AuthUtil 无法被 Mock 需要特殊处理），编写测试 1-4 的 RED 版本。

**测试类结构：**

```java
package cn.opensrcdevelop.auth.biz.service.permission.request;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.*;
import static org.mockito.Mockito.*;

import cn.opensrcdevelop.auth.biz.constants.PermissionRequestStatusEnum;
import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestCreateDto;
import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestResponseDto;
import cn.opensrcdevelop.auth.biz.mapper.permission.request.PermissionRequestItemMapper;
import cn.opensrcdevelop.auth.biz.mapper.permission.request.PermissionRequestMapper;
import cn.opensrcdevelop.auth.biz.repository.permission.request.PermissionRequestRepository;
import cn.opensrcdevelop.auth.biz.service.auth.AuthorizeService;
import cn.opensrcdevelop.auth.biz.service.permission.request.impl.PermissionRequestServiceImpl;
import cn.opensrcdevelop.auth.biz.util.AuthUtil;
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import java.util.List;
import org.junit.jupiter.api.Test;
import org.mockito.MockedStatic;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

@SpringBootTest
class PermissionRequestServiceTest {

    @MockBean
    private PermissionRequestMapper permissionRequestMapper;

    @MockBean
    private PermissionRequestItemMapper permissionRequestItemMapper;

    @MockBean
    private PermissionAutoApproveService permissionAutoApproveService;

    @MockBean
    private AuthorizeService authorizeService;

    @MockBean
    private PermissionRequestRepository permissionRequestRepository;

    @Autowired
    private PermissionRequestServiceImpl permissionRequestService;

    @Test
    void submitRequest_allPending_shouldReturnCorrectCounts() {
        // Arrange
        PermissionRequestCreateDto dto = new PermissionRequestCreateDto();
        dto.setPermissionIds(List.of("p1", "p2"));
        dto.setReason("业务需要");

        when(permissionAutoApproveService.isEnabled("p1")).thenReturn(false);
        when(permissionAutoApproveService.isEnabled("p2")).thenReturn(false);
        when(permissionRequestRepository.hasActivePendingRequest(anyString(), anyList())).thenReturn(false);
        when(permissionRequestMapper.insert(any())).thenReturn(1);
        when(permissionRequestItemMapper.insert(any())).thenReturn(1);

        try (MockedStatic<AuthUtil> authUtil = mockStatic(AuthUtil.class)) {
            authUtil.when(AuthUtil::getCurrentUserId).thenReturn("user-001");

            // Act
            PermissionRequestResponseDto result = permissionRequestService.submitRequest(dto);

            // Assert
            assertNotNull(result.getRequestId());
            assertEquals(0, result.getAutoApprovedCount());
            assertEquals(2, result.getPendingCount());
        }
    }

    @Test
    void submitRequest_partialAutoApprove_shouldCallAuthorizeService() {
        PermissionRequestCreateDto dto = new PermissionRequestCreateDto();
        dto.setPermissionIds(List.of("p1", "p2"));
        dto.setReason("业务需要");

        when(permissionAutoApproveService.isEnabled("p1")).thenReturn(true);
        when(permissionAutoApproveService.isEnabled("p2")).thenReturn(false);
        when(permissionRequestRepository.hasActivePendingRequest(anyString(), anyList())).thenReturn(false);
        when(permissionRequestMapper.insert(any())).thenReturn(1);
        when(permissionRequestItemMapper.insert(any())).thenReturn(1);

        try (MockedStatic<AuthUtil> authUtil = mockStatic(AuthUtil.class)) {
            authUtil.when(AuthUtil::getCurrentUserId).thenReturn("user-001");

            PermissionRequestResponseDto result = permissionRequestService.submitRequest(dto);

            assertEquals(1, result.getAutoApprovedCount());
            assertEquals(1, result.getPendingCount());
            verify(authorizeService, times(1)).authorize(
                    argThat(req -> req.getUserIds().contains("user-001")
                            && req.getPermissionIds().contains("p1")
                            && !req.getPermissionIds().contains("p2")));
        }
    }

    @Test
    void submitRequest_allAutoApprove_shouldSetStatusAutoApproved() {
        PermissionRequestCreateDto dto = new PermissionRequestCreateDto();
        dto.setPermissionIds(List.of("p1"));
        dto.setReason("业务需要");

        when(permissionAutoApproveService.isEnabled("p1")).thenReturn(true);
        when(permissionRequestRepository.hasActivePendingRequest(anyString(), anyList())).thenReturn(false);
        when(permissionRequestMapper.insert(any())).thenReturn(1);
        when(permissionRequestItemMapper.insert(any())).thenReturn(1);

        // 用 ArgumentCaptor 捕获传入 mapper.insert 的 PermissionRequest 对象，验证 status 字段
        org.mockito.ArgumentCaptor<cn.opensrcdevelop.auth.biz.entity.permission.request.PermissionRequest> captor =
                org.mockito.ArgumentCaptor.forClass(
                        cn.opensrcdevelop.auth.biz.entity.permission.request.PermissionRequest.class);

        try (MockedStatic<AuthUtil> authUtil = mockStatic(AuthUtil.class)) {
            authUtil.when(AuthUtil::getCurrentUserId).thenReturn("user-001");

            PermissionRequestResponseDto result = permissionRequestService.submitRequest(dto);

            assertEquals(1, result.getAutoApprovedCount());
            assertEquals(0, result.getPendingCount());
            verify(permissionRequestMapper).insert(captor.capture());
            assertEquals(PermissionRequestStatusEnum.AUTO_APPROVED.getCode(), captor.getValue().getStatus());
        }
    }

    @Test
    void submitRequest_duplicatePendingRequest_shouldThrowException() {
        PermissionRequestCreateDto dto = new PermissionRequestCreateDto();
        dto.setPermissionIds(List.of("p1"));
        dto.setReason("业务需要");

        when(permissionRequestRepository.hasActivePendingRequest(anyString(), anyList())).thenReturn(true);

        try (MockedStatic<AuthUtil> authUtil = mockStatic(AuthUtil.class)) {
            authUtil.when(AuthUtil::getCurrentUserId).thenReturn("user-001");

            // 期望抛出异常（业务异常，提示存在重复申请）
            assertThrows(RuntimeException.class, () -> permissionRequestService.submitRequest(dto));
            // 验证未调用 mapper.insert（未创建申请记录）
            verify(permissionRequestMapper, never()).insert(any());
        }
    }
}
```

**步骤 2：创建 PermissionRequestService 接口**

```java
package cn.opensrcdevelop.auth.biz.service.permission.request;

import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestCreateDto;
import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestResponseDto;

/**
 * 权限申请提交服务
 */
public interface PermissionRequestService {

    /**
     * 提交权限申请
     * 支持批量申请多个权限，共用一个申请理由（per D-01）
     * 开启自动批准的权限即时写入 t_authorize（per D-02, D-03）
     * 存在重复 PENDING 或 AUTO_APPROVED 申请时拒绝（per D-05）
     *
     * @param dto 申请请求（permissionIds + reason）
     * @return 申请结果（requestId + 各状态数量统计）
     */
    PermissionRequestResponseDto submitRequest(PermissionRequestCreateDto dto);
}
```

**步骤 3：创建 PermissionRequestServiceImpl 实现（GREEN）**

关键实现逻辑（完整方法体）：

```java
package cn.opensrcdevelop.auth.biz.service.permission.request.impl;

import cn.opensrcdevelop.auth.audit.annotation.Audit;
import cn.opensrcdevelop.auth.audit.enums.AuditType;
import cn.opensrcdevelop.auth.audit.enums.ResourceType;
import cn.opensrcdevelop.auth.audit.enums.SysOperationType;
import cn.opensrcdevelop.auth.biz.constants.PermissionRequestStatusEnum;
import cn.opensrcdevelop.auth.biz.dto.auth.AuthorizeRequestDto;
import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestCreateDto;
import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestResponseDto;
import cn.opensrcdevelop.auth.biz.entity.permission.request.PermissionRequest;
import cn.opensrcdevelop.auth.biz.entity.permission.request.PermissionRequestItem;
import cn.opensrcdevelop.auth.biz.mapper.permission.request.PermissionRequestItemMapper;
import cn.opensrcdevelop.auth.biz.mapper.permission.request.PermissionRequestMapper;
import cn.opensrcdevelop.auth.biz.repository.permission.request.PermissionRequestRepository;
import cn.opensrcdevelop.auth.biz.service.auth.AuthorizeService;
import cn.opensrcdevelop.auth.biz.service.permission.request.PermissionAutoApproveService;
import cn.opensrcdevelop.auth.biz.service.permission.request.PermissionRequestService;
import cn.opensrcdevelop.auth.biz.util.AuthUtil;
import cn.opensrcdevelop.common.util.CommonUtil;
import java.time.LocalDateTime;
import java.util.ArrayList;
import java.util.List;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
@RequiredArgsConstructor
public class PermissionRequestServiceImpl implements PermissionRequestService {

    private final PermissionRequestMapper permissionRequestMapper;
    private final PermissionRequestItemMapper permissionRequestItemMapper;
    private final PermissionAutoApproveService permissionAutoApproveService;
    private final AuthorizeService authorizeService;
    private final PermissionRequestRepository permissionRequestRepository;

    /**
     * 提交权限申请
     *
     * @param dto 申请请求
     * @return 申请结果
     */
    @Audit(
        type = AuditType.USER_OPERATION,
        resource = ResourceType.PERMISSION_REQUEST,
        sysOperation = SysOperationType.CREATE,
        success = "提交了权限申请（{{ #result.requestId }}）",
        fail = "提交权限申请失败"
    )
    @Transactional
    @Override
    public PermissionRequestResponseDto submitRequest(PermissionRequestCreateDto dto) {
        String userId = AuthUtil.getCurrentUserId();
        List<String> permissionIds = dto.getPermissionIds();

        // 1. 重复申请检查（per D-05）
        if (permissionRequestRepository.hasActivePendingRequest(userId, permissionIds)) {
            throw new IllegalStateException("您已有待审批或自动批准的相同权限申请，请勿重复提交");
        }

        String requestId = CommonUtil.getUUIDV7String();
        LocalDateTime now = LocalDateTime.now();

        // 2. 判断每个权限是否自动批准，构建申请明细列表
        List<PermissionRequestItem> items = new ArrayList<>();
        List<String> autoApprovedPermIds = new ArrayList<>();
        for (String permId : permissionIds) {
            boolean autoApproved = permissionAutoApproveService.isEnabled(permId);
            PermissionRequestItem item = new PermissionRequestItem();
            item.setItemId(CommonUtil.getUUIDV7String());
            item.setRequestId(requestId);
            item.setPermissionId(permId);
            item.setAutoApproved(autoApproved);
            items.add(item);
            if (autoApproved) {
                autoApprovedPermIds.add(permId);
            }
        }

        // 3. 确定申请主记录状态：全部自动批准则 AUTO_APPROVED，否则 PENDING
        boolean allAutoApproved = autoApprovedPermIds.size() == permissionIds.size();
        String status = allAutoApproved
                ? PermissionRequestStatusEnum.AUTO_APPROVED.getCode()
                : PermissionRequestStatusEnum.PENDING.getCode();

        // 4. 插入申请主记录（per D-03：在同一事务内）
        PermissionRequest request = new PermissionRequest();
        request.setRequestId(requestId);
        request.setUserId(userId);
        request.setReason(dto.getReason());
        request.setStatus(status);
        request.setRequestTime(now);
        permissionRequestMapper.insert(request);

        // 5. 插入申请明细记录（per D-04：批量全部成功或全部回滚）
        for (PermissionRequestItem item : items) {
            permissionRequestItemMapper.insert(item);
        }

        // 6. 自动批准权限写入 t_authorize（per PAUT-03，@CacheEvict 由 AuthorizeService 内部处理）
        if (!autoApprovedPermIds.isEmpty()) {
            AuthorizeRequestDto authDto = new AuthorizeRequestDto();
            authDto.setUserIds(List.of(userId));
            authDto.setPermissionIds(autoApprovedPermIds);
            // expressionIds 和 priority 保持 null，Phase 3 不设置限制条件
            authorizeService.authorize(authDto);
        }

        // 7. 构建并返回响应
        PermissionRequestResponseDto response = new PermissionRequestResponseDto();
        response.setRequestId(requestId);
        response.setAutoApprovedCount(autoApprovedPermIds.size());
        response.setPendingCount(permissionIds.size() - autoApprovedPermIds.size());
        return response;
    }
}
```

**步骤 4：运行测试（GREEN 验证）**
```bash
./gradlew :auth-biz:test --tests "*PermissionRequestServiceTest*" -x
```
所有 4 个测试必须通过。
  </action>

  <verify>
    <automated>./gradlew :auth-biz:test --tests "*PermissionRequestServiceTest*" 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - auth-biz/.../service/permission/request/PermissionRequestService.java 存在，包含 "submitRequest"
    - auth-biz/.../service/permission/request/impl/PermissionRequestServiceImpl.java 包含 "@Transactional", "@Audit", "authorizeService.authorize", "permissionAutoApproveService.isEnabled", "permissionRequestRepository.hasActivePendingRequest"
    - auth-biz/.../service/permission/request/impl/PermissionRequestServiceImpl.java 包含 "PermissionRequestStatusEnum.AUTO_APPROVED.getCode()"（全自动批准时 status 赋值）
    - auth-biz/src/test/...PermissionRequestServiceTest.java 存在，包含 4 个 @Test 方法
    - `./gradlew :auth-biz:test --tests "*PermissionRequestServiceTest*"` 退出码为 0，4 个测试全部通过
  </acceptance_criteria>

  <done>PermissionRequestService 接口和实现就绪，4 个单元测试全部通过（含重复申请检查、自动批准、@Audit 验证）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserCenterPermissionRequestController + 控制器测试 + authorize 权限码注册</name>
  <files>
    auth-server/src/main/java/cn/opensrcdevelop/auth/controller/user/UserCenterPermissionRequestController.java,
    auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/controller/UserCenterPermissionRequestControllerTest.java,
    auth-server/src/main/resources/application-authorize.properties
  </files>

  <read_first>
    - auth-server/src/main/java/cn/opensrcdevelop/auth/controller/PermissionController.java（@Tag、@RestController、@RestResponse、@RequestMapping、@Authorize 模式）
    - auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/controller/PermissionControllerTest.java（@WebMvcTest、MockMvc、@WithMockUser 测试模式）
    - auth-server/src/main/resources/application-authorize.properties（末尾，用于追加新权限码，先读取现有末尾内容以避免重复）
  </read_first>

  <behavior>
    - 测试 1: POST /user-center/permissions/requests 提交成功，返回 200 + requestId
      输入：{ "permissionIds": ["perm-001"], "reason": "业务需要" }
      期望：HTTP 200，响应体包含 $.requestId（非 null）

    - 测试 2: POST /user-center/permissions/requests 理由为空，返回 400
      输入：{ "permissionIds": ["perm-001"], "reason": "" }
      期望：HTTP 400（@Valid @NotBlank 触发）

    - 测试 3: POST /user-center/permissions/requests permissionIds 为空，返回 400
      输入：{ "permissionIds": [], "reason": "业务需要" }
      期望：HTTP 400（@Valid @NotEmpty 触发）
  </behavior>

  <action>
**步骤 1：创建控制器目录**

`auth-server/src/main/java/cn/opensrcdevelop/auth/controller/user/`

**步骤 2：创建 UserCenterPermissionRequestController.java**

```java
package cn.opensrcdevelop.auth.controller.user;

import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestCreateDto;
import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestResponseDto;
import cn.opensrcdevelop.auth.biz.service.permission.request.PermissionRequestService;
import cn.opensrcdevelop.auth.client.authorize.annoation.Authorize;
import cn.opensrcdevelop.common.annoation.RestResponse;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@Tag(name = "API-UserCenter-PermissionRequest", description = "接口-用户中心-权限申请")
@RestController
@RestResponse
@RequestMapping("/user-center/permissions/requests")
@RequiredArgsConstructor
public class UserCenterPermissionRequestController {

    private final PermissionRequestService permissionRequestService;

    @Operation(summary = "提交权限申请", description = "用户提交权限申请，支持批量申请多个权限，共用一个申请理由")
    @PostMapping
    @Authorize({"submitPermissionRequest"})
    public PermissionRequestResponseDto submitRequest(@RequestBody @Valid PermissionRequestCreateDto requestDto) {
        return permissionRequestService.submitRequest(requestDto);
    }
}
```

**步骤 3：创建控制器测试目录**

`auth-biz/src/test/java/cn/opensrcdevelop/auth/biz/controller/`（已存在，直接创建文件）

**步骤 4：创建 UserCenterPermissionRequestControllerTest.java（TDD - RED 先行）**

```java
package cn.opensrcdevelop.auth.biz.controller;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import cn.opensrcdevelop.auth.biz.dto.permission.request.PermissionRequestResponseDto;
import cn.opensrcdevelop.auth.biz.service.permission.request.PermissionRequestService;
import cn.opensrcdevelop.auth.controller.user.UserCenterPermissionRequestController;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.security.test.context.support.WithMockUser;
import org.springframework.test.web.servlet.MockMvc;

@WebMvcTest(controllers = UserCenterPermissionRequestController.class)
class UserCenterPermissionRequestControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private PermissionRequestService permissionRequestService;

    @Test
    @WithMockUser
    void submitRequest_validInput_shouldReturn200WithRequestId() throws Exception {
        PermissionRequestResponseDto response = new PermissionRequestResponseDto();
        response.setRequestId("req-001");
        response.setAutoApprovedCount(0);
        response.setPendingCount(1);

        when(permissionRequestService.submitRequest(any())).thenReturn(response);

        mockMvc.perform(post("/user-center/permissions/requests")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"permissionIds\":[\"perm-001\"],\"reason\":\"业务需要\"}"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.requestId").value("req-001"))
                .andExpect(jsonPath("$.autoApprovedCount").value(0))
                .andExpect(jsonPath("$.pendingCount").value(1));
    }

    @Test
    @WithMockUser
    void submitRequest_emptyReason_shouldReturn400() throws Exception {
        mockMvc.perform(post("/user-center/permissions/requests")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"permissionIds\":[\"perm-001\"],\"reason\":\"\"}"))
                .andExpect(status().isBadRequest());
    }

    @Test
    @WithMockUser
    void submitRequest_emptyPermissionIds_shouldReturn400() throws Exception {
        mockMvc.perform(post("/user-center/permissions/requests")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"permissionIds\":[],\"reason\":\"业务需要\"}"))
                .andExpect(status().isBadRequest());
    }
}
```

**步骤 5：修改 application-authorize.properties — 追加权限码**

在文件末尾追加以下内容（使用与现有格式完全一致的格式）：

```properties
# 权限申请
auth.client.authorize.submitPermissionRequest.permission=52cb8d26-a352-4e5c-99a7-d52b8afff3b1:permission:request:submit
```

**步骤 6：运行控制器测试（GREEN 验证）**

```bash
./gradlew :auth-biz:test --tests "*UserCenterPermissionRequestControllerTest*"
```

所有 3 个测试必须通过。
  </action>

  <verify>
    <automated>./gradlew :auth-biz:test --tests "*UserCenterPermissionRequestControllerTest*" 2>&1 | tail -20 && ./gradlew :auth-server:compileJava -x test 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - auth-server/.../controller/user/UserCenterPermissionRequestController.java 存在，包含 "@PostMapping", "@Authorize", "@Valid", "submitPermissionRequest"
    - auth-server/.../controller/user/UserCenterPermissionRequestController.java 包含 "@RequestMapping(\"/user-center/permissions/requests\")"
    - auth-biz/src/test/.../controller/UserCenterPermissionRequestControllerTest.java 存在，包含 3 个 @Test 方法
    - auth-server/src/main/resources/application-authorize.properties 包含 "submitPermissionRequest"
    - `./gradlew :auth-biz:test --tests "*UserCenterPermissionRequestControllerTest*"` 退出码为 0，3 个测试全部通过
    - `./gradlew :auth-server:compileJava -x test` 退出码为 0
  </acceptance_criteria>

  <done>UserCenterPermissionRequestController 就绪，3 个控制器测试通过，auth-server 编译无误，application-authorize.properties 包含 submitPermissionRequest 权限码</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Client → Controller | 外部用户请求经过 Spring Security 过滤器链后到达控制器 |
| Controller → Service | @Authorize 注解验证权限后调用 service |
| Service → AuthorizeService | 同一事务内写入 t_authorize，AuthorizeService 含 @CacheEvict |
| Service → SecurityContext | AuthUtil.getCurrentUserId() 从 SecurityContext 获取用户ID，不信任请求参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03P2-01 | Spoofing | UserCenterPermissionRequestController | mitigate | Spring Security 过滤器链要求用户已认证；@Authorize({"submitPermissionRequest"}) 验证权限码 |
| T-03P2-02 | Tampering | PermissionRequestServiceImpl — SQL injection via permissionIds/reason | mitigate | 所有数据库操作通过 MyBatis-Plus mapper.insert(entity) 参数化，LambdaQueryWrapper 避免字符串拼接 |
| T-03P2-03 | Elevation of Privilege | submitRequest — 用户提交他人权限申请 | mitigate | userId 强制从 AuthUtil.getCurrentUserId() 获取（SecurityContext），不接受请求体中的 userId 字段 |
| T-03P2-04 | Denial of Service | submitRequest — 重复申请刷请求 | mitigate | hasActivePendingRequest 检查已有 PENDING 或 AUTO_APPROVED 申请，重复则抛出异常拒绝（per D-05） |
| T-03P2-05 | Tampering | PermissionRequestServiceImpl — 未授权自动批准 | accept | 自动批准由管理员通过 t_permission_auto_approve 配置（Phase 8），用户无法直接控制；PermissionAutoApproveService 只读此配置 |
| T-03P2-06 | Information Disclosure | @Audit 日志泄露敏感信息 | mitigate | @Audit success 模板仅记录 requestId，不记录完整权限ID列表和申请理由；审计日志访问受管理员权限控制 |
</threat_model>

<verification>
全 Phase 3 验证：

```bash
# 1. 完整测试套件（全部 Phase 3 新增测试）
./gradlew :auth-biz:test --tests "*PermissionRequestServiceTest*" --tests "*UserCenterPermissionRequestControllerTest*"

# 2. 全模块测试
./gradlew :auth-biz:test

# 3. 编译验证（所有受影响模块）
./gradlew :auth-biz:compileJava :auth-audit:compileJava :auth-server:compileJava -x test

# 4. 关键合约验证
grep -n "PERMISSION_REQUEST" auth-audit/src/main/java/cn/opensrcdevelop/auth/audit/enums/ResourceType.java
grep -n "submitPermissionRequest" auth-server/src/main/resources/application-authorize.properties
grep -n "hasActivePendingRequest" auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/repository/permission/request/PermissionRequestRepository.java
grep -rn "@Audit" auth-biz/src/main/java/cn/opensrcdevelop/auth/biz/service/permission/request/impl/PermissionRequestServiceImpl.java

# 5. 代码格式化
./gradlew spotlessApply && ./gradlew spotlessCheck
```
</verification>

<success_criteria>
- POST /user-center/permissions/requests 端点存在（UserCenterPermissionRequestController）
- 7 个单元测试全部绿灯（4 个 Service 测试 + 3 个 Controller 测试）
- @Transactional + @Audit + hasActivePendingRequest 重复检查均在 PermissionRequestServiceImpl 中实现
- 全部自动批准时 PermissionRequest.status = "AUTO_APPROVED"，混合或全 PENDING 时 status = "PENDING"
- AuthorizeService.authorize() 在同一事务内被调用（自动批准权限写入 t_authorize）
- application-authorize.properties 包含 submitPermissionRequest 权限码
- `./gradlew :auth-biz:compileJava :auth-audit:compileJava :auth-server:compileJava -x test` 全部通过
- `./gradlew spotlessCheck` 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/03-user-request-submission/03-02-SUMMARY.md`，记录：
- 实现的端点：POST /user-center/permissions/requests
- 关键服务逻辑（@Transactional 边界、自动批准流程、重复检查）
- 测试覆盖：7 个测试（Service 4 + Controller 3）
- 权限码：submitPermissionRequest
- 全自动批准 status 策略：AUTO_APPROVED / 混合 status：PENDING
- @Audit 注解参数（type=USER_OPERATION, resource=PERMISSION_REQUEST, success 模板）
</output>
