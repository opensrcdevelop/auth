Please verify if the following SQL can answer the user's question.

### Original Question
${question}

### Generated SQL
${sql}

### Query Columns
<#list query_columns as column>
- ${column.column_name}: ${column.display_name}
</#list>

### Sample Query Result (JSON)
${sample_data}

### Review Viewpoint
${viewpoint}